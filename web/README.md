# KidQ Web

Next.js + TypeScript frontend for KidQ.

## Development

```bash
npm run dev
```

Runs at [http://localhost:3000](http://localhost:3000).

## Environment

Copy `.env.example` to `.env.local` and set `NEXT_PUBLIC_API_URL` to point at the running API.
