/**
 * P1 DPDP Consent Gate — stub route so ticket 02's routing can target a
 * real page. Built out for real by ticket 03 (.scratch/kidq-parent-experience/issues/03-consent-gate.md).
 */
export default function ConsentGatePage() {
  return (
    <main style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", padding: 24 }}>
      <p style={{ color: "var(--kq-text-secondary)" }}>
        P1 Consent Gate — routed here correctly. Built out in ticket 03.
      </p>
    </main>
  );
}
