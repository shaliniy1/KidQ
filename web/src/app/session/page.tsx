/**
 * P7a Start a Session — stub route so ticket 02's routing can target a
 * real page. Built out for real by ticket 07 (.scratch/kidq-parent-experience/issues/07-start-a-session.md).
 */
export default function StartSessionPage() {
  return (
    <main style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", padding: 24 }}>
      <p style={{ color: "var(--kq-text-secondary)" }}>
        P7a Start a Session — routed here correctly. Built out in ticket 07.
      </p>
    </main>
  );
}
