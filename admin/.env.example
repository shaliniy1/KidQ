NEXT_PUBLIC_API_URL=http://localhost:4000
# Supabase Auth for admin sign-in (the anon key is public by design; never put the service key here).
# Leave both empty locally to use API dev tokens (the API must run with AUTH_MODE=dev).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
