# KidQ API

TypeScript backend for KidQ.

## Development

```bash
npm run dev
```

Runs at [http://localhost:4000](http://localhost:4000).

## Endpoints

- `GET /health` → `{ "status": "ok", "service": "kidq-api" }`
